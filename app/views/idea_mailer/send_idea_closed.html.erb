<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <title>DriveAnIdea</title>
    <style type='text/css'>
    body, td { colour: #000000; font-size:14px; font-family:Verdana,Helvetica,Arial; }
    .divider { background-color:#555555; height:2px; }
    .left_pane h2 { margin-top:0; margin-bottom:8px; font-size:18px; }
    .left_pane p { margin:8px 0; }
    #footer { font-size:11px; color:#555555; }
    #footer a { color:#555555;}
    .table { background-color: #E1EDF7; }
    </style> 
  </head>
  <body>
    <table width="100%" border="0" cellpadding="0" cellspacing="0">
        <tr><td align="center" valign="top">
          <table width="650" id="main" border="0" cellpadding="0" cellspacing="20", class='table'>
            <tr>
              <td id="header" colspan="1">
                <h1>DriveAnIdea</h1>
              </td>
              <td>
                <h6>🔒 Idea Has Closed!</h6>
              </td>
            </tr>
            <tr>
              <td class="divider" colspan="2" bgcolor="#555555" height="2"></td>
            </tr>
            <tr class="left_pane">
              <td>
                <p><small class='text-muted'>Idea closed <%= time_ago_in_words(@idea.closing_time) %> ago.</small></p>
                <p><small>Idea created by: <%= get_username(@idea.user_id) %></small>
              </td>
              <td valign="top">
                <p>Voting Style: <b><%= @idea.voting_style %></b></p>
                <% if @idea.voting_style == 'Binary' %>
                  <p>                    
                    Votes: 
                    <% @votes.each do |vote| %>
                      <b><%= vote.value == 1 ? '👍' : '👎' %></b>                      
                    <% end %>
                    <small class='text-muted'>(<%= @votes.count %> total)</small>
                  </p>
                <% else %>
                  <p>
                    Votes: 
                    <% @votes.each do |vote| %>
                      <b><%= vote.value %>,</b>
                    <% end %>
                    <small class='text-muted'>(<%= @votes.count %> total)</small>
                  </p>
                  <p>
                    Average Vote: <b><%= @votes.average(:value) %></b>
                  </p>
                <% end %>
                
                <p>
                  Idea:
                  <i><small class='text-muted'><%= raw(@idea.text) %></small></i>
                </p>
                
              </td>
            </tr>
            <tr>
              <td class="divider" colspan="2" bgcolor="#555555" height="2"></td>
            </tr>
            <tr>
              <td id="footer" colspan="2" align="center">
                <p><%= link_to 'Click Here', idea_url(@idea) %> to review this idea.</p>
                <p>&copy;2017 Electronic Commerce Group Ltd.</p>
              </td>
            </tr>
          </table>
        </td></tr>
      </table>

  </body>
</html> 