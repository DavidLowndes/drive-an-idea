<h4 align ="center">	
	<div class = "scroller">
		<strong>Add a comment:</strong>
		<%= form_for([@idea, @idea.comments.build]) do |f| %>
			<p>
				<%= f.text_area :body %>
			</p>
			<p>
				<%= f.submit %>
			</p>
		<% end %>
	</div>
</h4>




