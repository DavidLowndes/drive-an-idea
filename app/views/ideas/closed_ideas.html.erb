<div class="container col-md-offset-1 col-md-10">
  <h1 align="center">Closed Ideas</h1>
  <%= render 'search' %>

  <% if @ideas.any? %>
    <%= render 'idea', obj: @ideas %>
  <% else %>
    <p align="center">
      There are no closed ideas!
    </p>
  <% end %>
</div>
