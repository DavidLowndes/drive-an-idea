<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <% if a.trackable %>
    <small class="text-muted"> A new Idea</small>
    <%= link_to a.trackable.text.html_safe, idea_path(a.trackable) %> was added by <%= link_to a.owner.full_name, user_path(a.owner) %>
  <% else %>
      An Idea was created, however it has since been deleted. 
  <% end %>
  <small class="text-muted"><%= time_ago_in_words(a.created_at) %> ago.</small>
</li>