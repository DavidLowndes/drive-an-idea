<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <% if a.trackable %>
    <small class="text-muted"> A new Comment</small>
    <%= link_to a.trackable.body.html_safe, idea_path(a.trackable) %> was added by <%= a.owner.full_name %>create
  <% else %>
      A Comment was created, however it has since been deleted. 
  <% end %>
  <small class="text-muted"><%= time_ago_in_words(a.created_at) %> ago.</small>
</li>